const form=document.querySelector('#hogan-form');const status=document.querySelector('#hogan-status');
form.addEventListener('submit',async(event)=>{event.preventDefault();if(!form.reportValidity())return;const button=form.querySelector('button[type=submit]');button.disabled=true;status.textContent='Enviando configuración…';const data=new FormData(form);data.append('_subject','Configuración piloto Farmacia HOGAN');data.append('_captcha','false');try{const response=await fetch('https://formsubmit.co/ajax/negocios@parmux.com',{method:'POST',headers:{Accept:'application/json'},body:data});if(!response.ok)throw new Error('send');form.reset();status.textContent='Recibido. PARMUX revisará la configuración por escrito antes de activar el piloto.';status.classList.add('is-success')}catch(error){status.textContent='No pudimos enviar el formulario. Revisa tu conexión o escribe a negocios@parmux.com.';status.classList.add('is-error')}finally{button.disabled=false}});
